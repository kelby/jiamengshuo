<%= form_for(@admin_catalog) do |f| %>
  <% if @admin_catalog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_catalog.errors.count, "error") %> prohibited this admin_catalog from being saved:</h2>

      <ul>
      <% @admin_catalog.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
